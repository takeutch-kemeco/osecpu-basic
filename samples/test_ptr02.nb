#include "stdoscp.nb"

/* 配列ポインターのテスト
 */

float padding[10];
float p;
float x;

float a[100];
a[50]=123;
p=&a[50];

__print(p@x);
__print(p);
__print(p<<16);

/* この時点で p は a[50] を指すので、
 * そこをベースと考えて p@a[0] とした場合は a[50] の位置を意味する。
 * また、たとえば p@a[99] とすれば、それは a[99 + 50] の位置を意味する。
 */
__print(p@a[0]);

a[99]=999;
__print(p@a[99 - 50]);
/* この場合でも a[99] の場所を指定する意味となる。
 * なぜならpがa[50]の位置を指しているので、 p@a[49] はすなわち a[99] と同じ位置を指すので。
 */

rem 最初と最後が0に成らなければ異常

dim a
a := 0
print a

a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1;
print a;

a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1;
print a;

a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1;
print a;

a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1;
print a;

a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1;
print a;

a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1;
print a

a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1;
print a

a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1;
print a

a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1;
print a

a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1;
print a

a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1;
print a

a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1;
print a

a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1;
print a

a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1;
print a

a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1; a := a - 1;
print a

a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1;
print a;

a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1;
print a;

a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1;
print a;

a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1;
print a;

a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1; a := a + 1;
print a;
